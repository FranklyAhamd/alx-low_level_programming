#include "main.h"
/**
 *_abs - a function that computes the absolute value of an integer
 *@ab: integer input
 *@ab: integer input
 */
int _abs(int c)
{
	if (c > 0 || c == 0)
	{
	return (c);
	}
	else
	retun (c *-1);
}
